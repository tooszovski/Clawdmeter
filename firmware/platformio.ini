[env:waveshare_amoled_216]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.38-1/platform-espressif32.zip
board = esp32-s3-devkitc-1
framework = arduino
board_build.arduino.memory_type = qio_opi
upload_speed = 921600
monitor_speed = 115200

; Compile shared code + this board's per-board folder; exclude all other
; boards. New ports add a `+<boards/<name>/>` line in their own env block.
build_src_filter =
    +<*>
    -<boards/>
    +<boards/waveshare_amoled_216/>

build_flags =
    -DBOARD_AMOLED_216
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DBOARD_HAS_PSRAM
    ; AXP2101 PMU
    -DXPOWERS_CHIP_AXP2101
    ; NimBLE config — peripheral, 2 simultaneous connections so the OS can
    ; hold the HID link (Space key from BOOT button) while the daemon holds
    ; its own connection for the custom data service.
    -DCONFIG_BT_NIMBLE_ROLE_BROADCASTER=1
    -DCONFIG_BT_NIMBLE_ROLE_PERIPHERAL=1
    -DCONFIG_BT_NIMBLE_ROLE_CENTRAL=0
    -DCONFIG_BT_NIMBLE_ROLE_OBSERVER=0
    -DCONFIG_BT_NIMBLE_MAX_CONNECTIONS=2
    ; Peripheral Preferred Connection Parameters (GAP 0x2A04) — the accessory's
    ; steady-state preference per Microsoft's Bluetooth accessory guidelines:
    ; interval 15-30ms (HID-snappy), latency 0, supervision timeout 6s.
    ; Captured on hardware: Windows honors a long timeout (9.6s) while only its
    ; HID driver holds the link, but clamps to 2s whenever an app GATT session
    ; (the daemon) is active — PPCP documents our preference and governs the
    ; HID-only steady state; it does NOT override that 2s clamp. Units:
    ; interval ×1.25ms, timeout ×10ms. Windows caches GATT per bond, so a
    ; re-pair is needed for changes here to be seen.
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_MIN_CONN_INTERVAL=12
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_MAX_CONN_INTERVAL=24
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_SLAVE_LATENCY=0
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_SUPERVISION_TMO=600
    ; LVGL config via build flags
    -DLV_CONF_SKIP
    -DLV_COLOR_DEPTH=16
    -DLV_USE_LOG=0
    -DLV_USE_BAR=1
    -DLV_USE_LABEL=1
    -DLV_USE_ARC=1
    -DLV_USE_BTN=1
    -DLV_USE_LINE=1
    -DLV_USE_OBJ=1
    -DLV_USE_IMG=1
    -DLV_USE_IMAGE=1
    -DLV_USE_ANIMIMG=0
    -DLV_TICK_CUSTOM=1
    -DLV_USE_SNAPSHOT=1

lib_deps =
    moononournation/GFX Library for Arduino@^1.5.6
    lewisxhe/SensorLib@^0.2.6
    lewisxhe/XPowersLib@^0.2.7
    lvgl/lvgl@^9.2.0
    bblanchon/ArduinoJson@^7.0.0
    h2zero/NimBLE-Arduino@^2.1.1


[env:waveshare_lcd_154]
; Waveshare ESP32-S3-Touch-LCD-1.54 — 240x240 ST7789 over plain 4-wire SPI
; (first non-QSPI/TFT port), CST816T touch, battery ADC (no PMU), ES8311
; speaker. Module is ESP32-S3R8: 16 MB quad flash + 8 MB octal PSRAM
; (esptool-verified: "Embedded PSRAM 8MB (AP_3v3)", eFuse flash type quad),
; hence qio_opi and the 16 MB partition layout.
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.38-1/platform-espressif32.zip
board = esp32-s3-devkitc-1
framework = arduino
board_build.arduino.memory_type = qio_opi
board_upload.flash_size = 16MB
board_upload.maximum_size = 16777216
board_build.partitions = default_16MB.csv
upload_speed = 921600
monitor_speed = 115200

build_src_filter =
    +<*>
    -<boards/>
    +<boards/waveshare_lcd_154/>

build_flags =
    -DBOARD_LCD_154
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DBOARD_HAS_PSRAM
    ; NimBLE config — peripheral, 2 simultaneous connections so the OS can
    ; hold the HID link (Space key from BOOT button) while the daemon holds
    ; its own connection for the custom data service.
    -DCONFIG_BT_NIMBLE_ROLE_BROADCASTER=1
    -DCONFIG_BT_NIMBLE_ROLE_PERIPHERAL=1
    -DCONFIG_BT_NIMBLE_ROLE_CENTRAL=0
    -DCONFIG_BT_NIMBLE_ROLE_OBSERVER=0
    -DCONFIG_BT_NIMBLE_MAX_CONNECTIONS=2
    ; LVGL config via build flags
    -DLV_CONF_SKIP
    -DLV_COLOR_DEPTH=16
    -DLV_USE_LOG=0
    -DLV_USE_BAR=1
    -DLV_USE_LABEL=1
    -DLV_USE_ARC=1
    -DLV_USE_BTN=1
    -DLV_USE_LINE=1
    -DLV_USE_OBJ=1
    -DLV_USE_IMG=1
    -DLV_USE_IMAGE=1
    -DLV_USE_ANIMIMG=0
    -DLV_TICK_CUSTOM=1
    -DLV_USE_SNAPSHOT=1

lib_deps =
    moononournation/GFX Library for Arduino@^1.6.4
    lvgl/lvgl@^9.2.0
    bblanchon/ArduinoJson@^7.0.0
    h2zero/NimBLE-Arduino@^2.1.1


[env:waveshare_lcd_4]
; Waveshare ESP32-S3-Touch-LCD-4 — 480x480 ST7701 RGB parallel + GT911 touch.
; First RGB-panel port. IO expander (0x24, TCA9554 / CH32V003) must init
; before gfx->begin() or the panel stays dark. Bounce buffers avoid DMA
; tearing against the PSRAM framebuffer. Module is ESP32-S3R8: 16 MB flash
; + 8 MB octal PSRAM, hence qio_opi and the 16 MB partition layout.
; No AXP2101 / IMU; KEY/PWR is hardware RST. Single BOOT button (GPIO 0).
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.38-1/platform-espressif32.zip
board = esp32-s3-devkitc-1
framework = arduino
board_build.arduino.memory_type = qio_opi
board_upload.flash_size = 16MB
board_upload.maximum_size = 16777216
board_build.partitions = default_16MB.csv
upload_speed = 921600
monitor_speed = 115200

build_src_filter =
    +<*>
    -<boards/>
    +<boards/waveshare_lcd_4/>

build_flags =
    -DBOARD_LCD_4
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DBOARD_HAS_PSRAM
    ; NimBLE config — peripheral, 2 simultaneous connections so the OS can
    ; hold the HID link (Space key from BOOT button) while the daemon holds
    ; its own connection for the custom data service.
    -DCONFIG_BT_NIMBLE_ROLE_BROADCASTER=1
    -DCONFIG_BT_NIMBLE_ROLE_PERIPHERAL=1
    -DCONFIG_BT_NIMBLE_ROLE_CENTRAL=0
    -DCONFIG_BT_NIMBLE_ROLE_OBSERVER=0
    -DCONFIG_BT_NIMBLE_MAX_CONNECTIONS=2
    ; LVGL config via build flags
    -DLV_CONF_SKIP
    -DLV_COLOR_DEPTH=16
    -DLV_USE_LOG=0
    -DLV_USE_BAR=1
    -DLV_USE_LABEL=1
    -DLV_USE_ARC=1
    -DLV_USE_BTN=1
    -DLV_USE_LINE=1
    -DLV_USE_OBJ=1
    -DLV_USE_IMG=1
    -DLV_USE_IMAGE=1
    -DLV_USE_ANIMIMG=0
    -DLV_TICK_CUSTOM=1
    -DLV_USE_SNAPSHOT=1

lib_deps =
    moononournation/GFX Library for Arduino@^1.6.4
    lewisxhe/SensorLib@^0.2.6
    lvgl/lvgl@^9.2.0
    bblanchon/ArduinoJson@^7.0.0
    h2zero/NimBLE-Arduino@^2.1.1


[env:waveshare_amoled_18]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.38-1/platform-espressif32.zip
board = esp32-s3-devkitc-1
framework = arduino
board_build.arduino.memory_type = qio_opi
board_upload.flash_size = 16MB
board_upload.maximum_size = 16777216
board_build.partitions = default_16MB.csv
upload_speed = 921600
monitor_speed = 115200

; Compile shared code + this board's per-board folder; exclude all other
; boards. New ports add a `+<boards/<name>/>` line in their own env block.
build_src_filter =
    +<*>
    -<boards/>
    +<boards/waveshare_amoled_18/>

build_flags =
    -DBOARD_AMOLED_18
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DBOARD_HAS_PSRAM
    ; AXP2101 PMU is present on the AMOLED-1.8 board too (battery is optional kit add-on)
    -DXPOWERS_CHIP_AXP2101
    ; NimBLE config — peripheral, 2 simultaneous connections so the OS can
    ; hold the HID link (Space key from BOOT button) while the daemon holds
    ; its own connection for the custom data service.
    -DCONFIG_BT_NIMBLE_ROLE_BROADCASTER=1
    -DCONFIG_BT_NIMBLE_ROLE_PERIPHERAL=1
    -DCONFIG_BT_NIMBLE_ROLE_CENTRAL=0
    -DCONFIG_BT_NIMBLE_ROLE_OBSERVER=0
    -DCONFIG_BT_NIMBLE_MAX_CONNECTIONS=2
    ; Peripheral Preferred Connection Parameters — see the waveshare_amoled_216
    ; env for the full rationale (Windows supervision-timeout churn fix).
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_MIN_CONN_INTERVAL=12
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_MAX_CONN_INTERVAL=24
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_SLAVE_LATENCY=0
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_SUPERVISION_TMO=600
    ; LVGL config via build flags
    -DLV_CONF_SKIP
    -DLV_COLOR_DEPTH=16
    -DLV_USE_LOG=0
    -DLV_USE_BAR=1
    -DLV_USE_LABEL=1
    -DLV_USE_ARC=1
    -DLV_USE_BTN=1
    -DLV_USE_LINE=1
    -DLV_USE_OBJ=1
    -DLV_USE_IMG=1
    -DLV_USE_IMAGE=1
    -DLV_USE_ANIMIMG=0
    -DLV_TICK_CUSTOM=1
    -DLV_USE_SNAPSHOT=1

lib_deps =
    ; 1.6.4+ ships Arduino_SH8601 in mainline
    moononournation/GFX Library for Arduino@^1.6.4
    ; SensorLib still used for QMI8658 IMU on AMOLED-1.8
    lewisxhe/SensorLib@^0.2.6
    lewisxhe/XPowersLib@^0.2.7
    lvgl/lvgl@^9.2.0
    bblanchon/ArduinoJson@^7.0.0
    h2zero/NimBLE-Arduino@^2.1.1


[env:waveshare_amoled_216_c6]
; C6 sibling of the AMOLED-2.16. Same panel/touch/PMU/IMU, no PSRAM,
; single-core RISC-V, BLE 5.3 only (no classic BT). If the build fails
; resolving the C6 board, bump pioarduino to a newer 55.x release that
; ships esp32-c6-devkitc-1.
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.38-1/platform-espressif32.zip
board = esp32-c6-devkitc-1
framework = arduino
upload_speed = 921600
monitor_speed = 115200
; Waveshare's C6-AMOLED-2.16 ships with 16 MB flash. The default 1.25 MB
; app partition can't hold this firmware (~1.43 MB) — switch to the
; 16 MB layout that gives the app ~6.5 MB, matching the AMOLED-1.8 env.
board_upload.flash_size = 16MB
board_upload.maximum_size = 16777216
board_build.partitions = default_16MB.csv

build_src_filter =
    +<*>
    -<boards/>
    +<boards/waveshare_amoled_216_c6/>

build_flags =
    -DBOARD_AMOLED_216_C6
    ; C6 has only USB-Serial-JTAG (HWCDC), no native USB-OTG. The Arduino
    ; core maps Serial → HWCDCSerial only when BOTH flags below are set;
    ; CDC_ON_BOOT alone selects USBSerial (TinyUSB) which doesn't exist on
    ; C6 and breaks the build. Without either flag, Serial routes to UART0
    ; on physical pins and nothing reaches the USB host.
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DARDUINO_USB_MODE=1
    ; No -DBOARD_HAS_PSRAM — C6 has no external PSRAM. Shared code
    ; (main.cpp, splash.cpp) gates on this to pick MALLOC_CAP_INTERNAL
    ; and shrink LVGL buffers + splash canvas accordingly.
    -DXPOWERS_CHIP_AXP2101
    -DCONFIG_BT_NIMBLE_ROLE_BROADCASTER=1
    -DCONFIG_BT_NIMBLE_ROLE_PERIPHERAL=1
    -DCONFIG_BT_NIMBLE_ROLE_CENTRAL=0
    -DCONFIG_BT_NIMBLE_ROLE_OBSERVER=0
    -DCONFIG_BT_NIMBLE_MAX_CONNECTIONS=2
    ; Peripheral Preferred Connection Parameters — see the waveshare_amoled_216
    ; env for the full rationale (Windows supervision-timeout churn fix).
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_MIN_CONN_INTERVAL=12
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_MAX_CONN_INTERVAL=24
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_SLAVE_LATENCY=0
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_SUPERVISION_TMO=600
    -DLV_CONF_SKIP
    -DLV_COLOR_DEPTH=16
    -DLV_USE_LOG=0
    -DLV_USE_BAR=1
    -DLV_USE_LABEL=1
    -DLV_USE_ARC=1
    -DLV_USE_BTN=1
    -DLV_USE_LINE=1
    -DLV_USE_OBJ=1
    -DLV_USE_IMG=1
    -DLV_USE_IMAGE=1
    -DLV_USE_ANIMIMG=0
    -DLV_TICK_CUSTOM=1
    ; Screenshot capture needs a full-frame RGB565 buffer (~460 KB on
    ; 480×480) which doesn't fit in C6 internal SRAM. send_screenshot
    ; prints SCREENSHOT_UNSUPPORTED on this board.
    -DLV_USE_SNAPSHOT=0

lib_deps =
    ; this board uses the CO5300 (same controller as the S3 2.16); Arduino_CO5300
    moononournation/GFX Library for Arduino@^1.6.4
    lewisxhe/SensorLib@^0.2.6
    lewisxhe/XPowersLib@^0.2.7
    lvgl/lvgl@^9.2.0
    bblanchon/ArduinoJson@^7.0.0
    h2zero/NimBLE-Arduino@^2.1.1


[env:waveshare_amoled_18_c6]
; C6 sibling of the AMOLED-1.8. Same 368x448 SH8601 panel + FT3168 touch as
; the S3 1.8, but on the C6 SoC: no PSRAM, single-core RISC-V, BLE 5 only.
; Display/touch power is gated by a TCA9554 IO expander (P4/P5); PWR button
; is the AXP2101 PKEY. If the build fails resolving the C6 board, bump
; pioarduino to a newer 55.x release that ships esp32-c6-devkitc-1.
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.38-1/platform-espressif32.zip
board = esp32-c6-devkitc-1
framework = arduino
upload_speed = 921600
monitor_speed = 115200
; Waveshare's C6-AMOLED-1.8 ships with 16 MB flash. The default 1.25 MB app
; partition can't hold this firmware — switch to the 16 MB layout that gives
; the app ~6.5 MB, matching the other 16 MB envs.
board_upload.flash_size = 16MB
board_upload.maximum_size = 16777216
board_build.partitions = default_16MB.csv

build_src_filter =
    +<*>
    -<boards/>
    +<boards/waveshare_amoled_18_c6/>

build_flags =
    -DBOARD_AMOLED_18_C6
    ; C6 has only USB-Serial-JTAG (HWCDC), no native USB-OTG. The Arduino
    ; core maps Serial → HWCDCSerial only when BOTH flags below are set;
    ; CDC_ON_BOOT alone selects USBSerial (TinyUSB) which doesn't exist on
    ; C6 and breaks the build.
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DARDUINO_USB_MODE=1
    ; No -DBOARD_HAS_PSRAM — C6 has no external PSRAM. Shared code
    ; (main.cpp, splash.cpp) gates on this to pick MALLOC_CAP_INTERNAL
    ; and shrink LVGL buffers + splash canvas accordingly.
    -DXPOWERS_CHIP_AXP2101
    -DCONFIG_BT_NIMBLE_ROLE_BROADCASTER=1
    -DCONFIG_BT_NIMBLE_ROLE_PERIPHERAL=1
    -DCONFIG_BT_NIMBLE_ROLE_CENTRAL=0
    -DCONFIG_BT_NIMBLE_ROLE_OBSERVER=0
    -DCONFIG_BT_NIMBLE_MAX_CONNECTIONS=2
    ; Peripheral Preferred Connection Parameters — see the waveshare_amoled_216
    ; env for the full rationale (Windows supervision-timeout churn fix).
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_MIN_CONN_INTERVAL=12
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_MAX_CONN_INTERVAL=24
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_SLAVE_LATENCY=0
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_SUPERVISION_TMO=600
    -DLV_CONF_SKIP
    -DLV_COLOR_DEPTH=16
    -DLV_USE_LOG=0
    -DLV_USE_BAR=1
    -DLV_USE_LABEL=1
    -DLV_USE_ARC=1
    -DLV_USE_BTN=1
    -DLV_USE_LINE=1
    -DLV_USE_OBJ=1
    -DLV_USE_IMG=1
    -DLV_USE_IMAGE=1
    -DLV_USE_ANIMIMG=0
    -DLV_TICK_CUSTOM=1
    ; Screenshot capture needs a full-frame RGB565 buffer that doesn't fit in
    ; C6 internal SRAM. send_screenshot prints SCREENSHOT_UNSUPPORTED instead.
    -DLV_USE_SNAPSHOT=0

lib_deps =
    ; 1.6.4+ required for Arduino_SH8601
    moononournation/GFX Library for Arduino@^1.6.4
    lewisxhe/SensorLib@^0.2.6
    lewisxhe/XPowersLib@^0.2.7
    lvgl/lvgl@^9.2.0
    bblanchon/ArduinoJson@^7.0.0
    h2zero/NimBLE-Arduino@^2.1.1


[env:waveshare_amoled_206]
; Waveshare ESP32-S3-Touch-AMOLED-2.06 — 410x502 watch form factor.
; CO5300 panel (as on the S3 2.16) + FT3168 touch (as on the S3 1.8), AXP2101
; PKEY for PWR, no IO expander. Ships with 32 MB flash.
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.38-1/platform-espressif32.zip
board = esp32-s3-devkitc-1
framework = arduino
board_build.arduino.memory_type = qio_opi
board_upload.flash_size = 32MB
board_upload.maximum_size = 33554432
board_build.partitions = default_32MB.csv
upload_speed = 921600
monitor_speed = 115200

build_src_filter =
    +<*>
    -<boards/>
    +<boards/waveshare_amoled_206/>

build_flags =
    -DBOARD_AMOLED_206
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DBOARD_HAS_PSRAM
    ; AXP2101 PMU
    -DXPOWERS_CHIP_AXP2101
    ; NimBLE config — peripheral, 2 simultaneous connections so the OS can
    ; hold the HID link (Space key from BOOT button) while the daemon holds
    ; its own connection for the custom data service.
    -DCONFIG_BT_NIMBLE_ROLE_BROADCASTER=1
    -DCONFIG_BT_NIMBLE_ROLE_PERIPHERAL=1
    -DCONFIG_BT_NIMBLE_ROLE_CENTRAL=0
    -DCONFIG_BT_NIMBLE_ROLE_OBSERVER=0
    -DCONFIG_BT_NIMBLE_MAX_CONNECTIONS=2
    ; Peripheral Preferred Connection Parameters — see the waveshare_amoled_216
    ; env for the full rationale (Windows supervision-timeout churn fix).
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_MIN_CONN_INTERVAL=12
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_MAX_CONN_INTERVAL=24
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_SLAVE_LATENCY=0
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_SUPERVISION_TMO=600
    ; LVGL config via build flags
    -DLV_CONF_SKIP
    -DLV_COLOR_DEPTH=16
    -DLV_USE_LOG=0
    -DLV_USE_BAR=1
    -DLV_USE_LABEL=1
    -DLV_USE_ARC=1
    -DLV_USE_BTN=1
    -DLV_USE_LINE=1
    -DLV_USE_OBJ=1
    -DLV_USE_IMG=1
    -DLV_USE_IMAGE=1
    -DLV_USE_ANIMIMG=0
    -DLV_TICK_CUSTOM=1
    -DLV_USE_SNAPSHOT=1

lib_deps =
    ; 1.6.4+ is required by Arduino ESP32 Core 3.x (spiFrequencyToClockDiv
    ; signature changed); 1.5.x fails to compile against this platform.
    moononournation/GFX Library for Arduino@^1.6.4
    lewisxhe/SensorLib@^0.2.6
    lewisxhe/XPowersLib@^0.2.7
    lvgl/lvgl@^9.2.0
    bblanchon/ArduinoJson@^7.0.0
    h2zero/NimBLE-Arduino@^2.1.1


[env:sim]
; Native desktop simulator — an SDL2 window instead of a panel, mouse as
; touch, keyboard as buttons, JSONL scenario playback instead of BLE. See
; boards/sim/board.h for the key map and scenario format.
; Requires SDL2 dev headers (sdl2-config on PATH): apt install libsdl2-dev.
platform = native

build_src_filter =
    +<*>
    -<boards/>
    +<boards/sim/>
    ; transport + audio are hardware-only: boards/sim/ble_sim.cpp stubs
    ; ble.h with scenario playback, and the sim's sound HAL no-ops.
    -<ble.cpp>
    -<chime.cpp>
    -<es8311.c>

build_flags =
    -DBOARD_SIM
    ; macOS: `g++` is Apple clang, which defaults to C++98 without an explicit
    ; -std and fails on constexpr / brace-init. Linux g++ (real GCC) defaults
    ; to gnu++17, which is why this only bites on macOS.
    -std=gnu++17
    ; behave like the PSRAM-equipped S3 targets (big LVGL strips, full
    ; splash canvas, screenshot path compiled) — the heap shim maps every
    ; capability to plain malloc anyway
    -DBOARD_HAS_PSRAM
    -Isrc/boards/sim/shim
    -lm
    !sdl2-config --cflags --libs
    ; LVGL config — identical to the hardware envs
    -DLV_CONF_SKIP
    -DLV_COLOR_DEPTH=16
    -DLV_USE_LOG=0
    -DLV_USE_BAR=1
    -DLV_USE_LABEL=1
    -DLV_USE_ARC=1
    -DLV_USE_BTN=1
    -DLV_USE_LINE=1
    -DLV_USE_OBJ=1
    -DLV_USE_IMG=1
    -DLV_USE_IMAGE=1
    -DLV_USE_ANIMIMG=0
    -DLV_TICK_CUSTOM=1
    -DLV_USE_SNAPSHOT=1

lib_deps =
    lvgl/lvgl@^9.2.0
    bblanchon/ArduinoJson@^7.0.0


[env:sim_long]
; Desktop simulator at the T-Display-S3-Long's landscape geometry (640x180) so
; the wide two-column layout can be iterated without hardware. Same SDL2
; backend and controls as `sim`; scenario: sim/scenario_long.jsonl
; (SIM_SCENARIO=sim/scenario_long.jsonl).
extends = env:sim
build_flags =
    ${env:sim.build_flags}
    -DLCD_WIDTH=640
    -DLCD_HEIGHT=180
    -DBOARD_NAME=\"Simulator\ 640x180\"


[env:lilygo_tdisplay_s3_long]
; LilyGo T-Display-S3-Long — 3.4" 180x640 AXS15231B QSPI LCD used in landscape
; (640x180, CPU-rotated strips), touch built into the AXS15231B (I2C 0x3B),
; PWM backlight on GPIO1, SY6970 charger (I2C 0x6A) for VBUS/charge status,
; VBAT divider on GPIO2. One usable button (BOOT/GPIO0 — the other is RESET),
; mapped to the PWR role: short press cycles animations / brightness, hold 3 s
; + release = pairing mode. Module is ESP32-S3R8: 16 MB flash + 8 MB OPI PSRAM.
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.38-1/platform-espressif32.zip
board = esp32-s3-devkitc-1
framework = arduino
board_build.arduino.memory_type = qio_opi
board_upload.flash_size = 16MB
board_upload.maximum_size = 16777216
board_build.partitions = default_16MB.csv
upload_speed = 921600
monitor_speed = 115200

build_src_filter =
    +<*>
    -<boards/>
    +<boards/lilygo_tdisplay_s3_long/>
    ; no speaker on this board
    -<chime.cpp>
    -<es8311.c>

build_flags =
    -DBOARD_TDISPLAY_S3_LONG
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DBOARD_HAS_PSRAM
    ; NimBLE config — peripheral, 2 simultaneous connections so the OS can
    ; hold the HID link while the daemon holds its own connection for the
    ; custom data service.
    -DCONFIG_BT_NIMBLE_ROLE_BROADCASTER=1
    -DCONFIG_BT_NIMBLE_ROLE_PERIPHERAL=1
    -DCONFIG_BT_NIMBLE_ROLE_CENTRAL=0
    -DCONFIG_BT_NIMBLE_ROLE_OBSERVER=0
    -DCONFIG_BT_NIMBLE_MAX_CONNECTIONS=2
    ; Multi-account payloads run ~250 bytes; lift the ATT MTU so a single
    ; write-without-response carries the whole JSON.
    -DCONFIG_BT_NIMBLE_ATT_PREFERRED_MTU=517
    ; Peripheral Preferred Connection Parameters — see the waveshare_amoled_216
    ; env for the full rationale (Windows supervision-timeout churn fix).
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_MIN_CONN_INTERVAL=12
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_MAX_CONN_INTERVAL=24
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_SLAVE_LATENCY=0
    -DMYNEWT_VAL_BLE_SVC_GAP_PPCP_SUPERVISION_TMO=600
    ; LVGL config via build flags
    -DLV_CONF_SKIP
    -DLV_COLOR_DEPTH=16
    -DLV_USE_LOG=0
    -DLV_USE_BAR=1
    -DLV_USE_LABEL=1
    -DLV_USE_ARC=1
    -DLV_USE_BTN=1
    -DLV_USE_LINE=1
    -DLV_USE_OBJ=1
    -DLV_USE_IMG=1
    -DLV_USE_IMAGE=1
    -DLV_USE_ANIMIMG=0
    -DLV_TICK_CUSTOM=1
    -DLV_USE_SNAPSHOT=1

lib_deps =
    ; Arduino_AXS15231B (with the 180x640 init sequence) needs 1.6.x
    moononournation/GFX Library for Arduino@^1.6.4
    lvgl/lvgl@^9.2.0
    bblanchon/ArduinoJson@^7.0.0
    h2zero/NimBLE-Arduino@^2.1.1
